# Copy to .env and fill in, OR just export ANTHROPIC_API_KEY in your shell.
# (If you've run `ant auth login`, the SDK picks up that profile and you can
#  leave this unset.)
ANTHROPIC_API_KEY=sk-ant-...
